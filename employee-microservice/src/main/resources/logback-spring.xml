<?xml version="1.0" encoding="UTF-8"?>
<!--
  logback-spring.xml — Structured Logging for ELK Stack.

  Interview: "How do you ship logs to ELK?"
    → "We use logstash-logback-encoder to emit structured JSON logs.
       In dev, logs go to console. In prod, a LogstashTcpSocketAppender
       sends them directly to Logstash over TCP. Logstash forwards
       to Elasticsearch, and Kibana visualizes them. MDC fields
       (traceId, spanId) are auto-included for distributed tracing."
-->
<configuration>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="employee-service"/>

    <!-- ── Console Appender (dev) — human-readable ───────────────────────────── -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] [%X{spanId:-}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── JSON Console Appender — structured for Docker/K8s log collectors ──── -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <customFields>{"service":"${APP_NAME}"}</customFields>
        </encoder>
    </appender>

    <!-- ── Logstash TCP Appender (prod) — ships logs to Logstash:5000 ────────── -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${LOGSTASH_HOST:-localhost}:${LOGSTASH_PORT:-5000}</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <customFields>{"service":"${APP_NAME}"}</customFields>
        </encoder>
        <!-- Reconnect if Logstash is temporarily unavailable -->
        <reconnectionDelay>5 seconds</reconnectionDelay>
        <keepAliveDuration>5 minutes</keepAliveDuration>
    </appender>

    <!-- ── Profile-based activation ──────────────────────────────────────────── -->
    <springProfile name="dev,default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.example.employee" level="DEBUG"/>
    </springProfile>

    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
            <appender-ref ref="LOGSTASH"/>
        </root>
        <logger name="com.example.employee" level="INFO"/>
    </springProfile>
</configuration>
